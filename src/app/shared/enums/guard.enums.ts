/**
 * @Info: The EGuard & ELaunchDarklyGuard Permissions is set in user.effects.ts
 */

export enum ERoleGuard {
  ADMIN_TOOLS = 'adminTools',
  DASHBOARD = 'dashboard',
  MANAGE = 'manage',
  SHIPMENT_ORDERS = 'shipmentOrders'
}

export enum ELaunchDarklyGuard {
  SO_MANAGEMENT = 'soManagement'
}
